# 検証報告 — 部活運営アプリ v0.9

実行日：2026-09-06

## 判定

**配車の操作導線、予定画像、グループ・アカウント・招待画面、LINEログインの接続基盤を追加した実装版。今回実行した検査は266 / 266合格。**

LINEの実設定・実アカウント認証、公開環境・実端末・OS共有、予定Site現在ソースとの完全比較は未完了。本番公開完了という判定ではない。GitHubやSiteへの書込み・デプロイは行っていない。

## 実行結果

| 検査 | 結果 | 実行対象 |
|---|---:|---|
| 既存サーバー・権限・共有DB | 23 / 23 | tests/test_server.py |
| LINE基盤・アカウント・招待 | 20 / 20 | tests/test_identity.py |
| 計算・予定・出欠・配車 | 23 / 23 | tests/domain.test.cjs |
| 既存画面・操作・表示幅 | 107 / 107 | tests/browser_checks.py |
| 既存の画面→API/SQLite連携 | 15 / 15 | tests/browser_server.py |
| 新しい画面・操作・表示幅 | 60 / 60 | tests/workflow_browser.py |
| 招待・アカウントの画面→API/SQLite連携 | 16 / 16 | tests/workflow_server.py |
| タッチ配車エミュレーション | 1 / 1 | tests/touch_check.py |
| 配布用単一HTML | 1 / 1 | tests/bundle_check.py |
| 合計 | **266 / 266** | qa/FINAL_TEST_SUMMARY.json |

合計は表示幅を含む検査項目の合算であり、266種類の独立した利用シナリオという意味ではない。旧メニュー計算の200入力比較はドメイン検査1件に含め、別に加算していない。

JavaScriptはdomain.js / base.js / app.js / workflow.js / sw.jsでnode --check成功。Pythonの既存＋追加テストは同一実行で43件成功。ログとJSONはqa内に保存した。まとめて実行した一度のシェル処理が時間制限で途中終了したため、後続のAPI連携・タッチ・単一HTMLを改めて実行し、最終ログを保存した。

## 配車で確認したこと

常時表示の往復2択、分割時の配置保持、帰りだけの編集、再統合前の明示的な元配置選択、再統合のUndo。過去の配車選択だけでは書き換えず、確認後に適用する。欠席者を除外し、コピー元を不変にし、対象の単価・利用日・通知を維持する。配車不要の指定と個別精算調整は確認文どおりリセットする。コピーUndoで適用前を復元する。月次固定された配車の新しい操作経路も無効。

ドライバー名の列見出しを維持し、PNGを実Canvasから生成。保存と共有の別ボタンを確認。未対応環境で共有成功を偽装しない。実OSの保存先への保存結果は判定していない。

## 予定で確認したこと

部活のみ／幹部込み切替とタスク期限の投影を維持。幹部表示中でも画像の初期対象は部活のみ。意図的に幹部込みを選べる。対象月・共有範囲をファイル名に示す。画面表示の＋Nで出力内容を省略しない既存描画処理を継続し、実PNGを保存した。

## アカウント・招待で確認したこと

ログイン→グループ選択→既存5タブ、1グループでも選択可能、複数グループの分離、空グループの出欠表示、アカウント表示名の保存、デモと実ログインの分離。

サーバーで実発行した招待を、別のTestClient/ブラウザ画面で確認→ログイン→参加。確認だけでリンクを消費せず、ログインだけでも加入しない。明示参加で所属を作り、部員名簿には追加しない。初期の機能編集権限は空。招待を受けた本人には発行権限がなく、オーナーが画面から指定した機能だけ編集可能になる。既存参加者は招待を消費しない。無効化でリンクが使えなくなり、画面に再発行案内を出す。

共有データの競合は拒否し、再読込で最新状態へ回復する。デモを終了すると共有版のログイン状態へ戻れる。画面内のLINEボタンは設定不足なら無効で、デモがLINE認証済みを装わない。

## LINEの検査範囲

state、nonce、PKCE S256、Secure/HttpOnlyフローCookie、期限切れ・再利用・キャンセル拒否、IDトークンのaud/nonce/iss/exp/sub/iatチェック、自前セッション、重複ID対応、既存アカウントへの明示連携、別アカウントとの衝突拒否を検査した。外部LINEのtoken/verify応答はモックに置き換えている。チャネル秘密値が公開configへ出ないことを確認した。

**実際のLINEアカウントでのログインは行っていない。** `.env.example`は空欄・無効のまま。接続後の確認はdocs/CODEX_HANDOFF_v0.9.mdに残してある。

## 発見して修正した点

新しいグループ切替の未保存保護が従来の「再読込」も止めてしまう問題を修正。共有版からデモに入ると実アカウントへ戻れなくなる問題を修正。コピーの説明と一致するように対象の単価・利用日・通知を維持し、個別調整のリセットを明記。空グループの出欠画面、表示名保存時の競合保護、中止済み配車の登録ボタンも補正した。

## 画面の確認

320 / 390 / 768pxで新しい主要8画面の横はみ出しを確認。既存画面の360 / 390 / 430 / 768 / 1280px検査も再実行した。画像は生成AIのイメージではなく、実装をテストブラウザへ描画したスクリーンショット／実Canvas画像。

代表：qa/v09_login.png、v09_groups.png、v09_account.png、v09_operators.png、v09_invite_server.png、v09_carpool_same.png、v09_carpool_separate.png、v09_carpool_merge.png、v09_carpool_copy.png、v09_carpool_image.png、v09_carpool_export.png、v09_calendar_options.png、v09_calendar_image.png、v09_calendar_export.png。

招待画面中のリンクは一時テストDBのtestserver向け。実サービスに参加できる招待ではない。画像中の日付・名前はサンプル／テスト用データ。

## 参照元

指定メニューGitHubのmainを再取得し、HEAD 53d72586964a159682907196ce076bb517a47625を確認。前回v0.8と同じだったため、練習メニューはその実装を維持して回帰検査した。

予定Siteとメニュー公開ページへのWeb取得は失敗、コンテナからもDNS取得に失敗。予定Siteの今の画面やコードとの完全一致は検証できていない。取得不能を、予定画像の新規実装や幹部カレンダーの回帰検査と混同しない。

## 環境上の限界

管理されたChromiumで通常のlocalhostへの遷移を試したがnet::ERR_BLOCKED_BY_ADMINISTRATOR。ブラウザポリシーを変更していない。検査はHTML/JavaScriptの直接描画とテスト用Storageアダプター、FastAPI TestClientへの通信橋渡しを使用した。SQLiteは実ファイルだがテスト用の一時DB。

実ブラウザのHTTP/HTTPS通信・Cookieポリシー・永続Storage・Service Worker導入更新・スマートフォン実機・OS共有シート・Push到達を検査したことにはならない。LINE実設定、公開負荷試験、セキュリティ監査、アカウント削除/認可取消運用も残る。
