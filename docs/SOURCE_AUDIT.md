# 参照元の確認記録

確認日：2026-09-06

## v0.9での再確認

GitHubコネクターで `/repos/manatocookietwitter-lang/training-menu-app/commits/main` を再取得した。HEADは `53d72586964a159682907196ce076bb517a47625` のままで、v0.8以降の追加コミットはこの時点ではない。以下のv0.8で反映したメニューUI・計算はそのまま維持した。「メニューを今回さらに最新版へ更新した」とは区別する。

提供された2つの公開ページは今回Web取得がInternal Error、コンテナからもDNS取得に失敗した。予定Siteの現在画面／バンドル／サーバーソースは未取得。現行Siteと同一と主張せず、既存統合版の幹部カレンダーを維持し、画像出力導線だけ改善した。

LINEは公式の組込み・PKCE・APIリファレンスを参照。外部LINEサービスへの接続設定や実認証は行っていない。URLは `CODEX_HANDOFF_v0.9.md` の参照欄に記載。

以下はv0.8時点での照合記録。

## 練習メニュー — 現行GitHubを確認済み

ユーザー指定の公開版は次です。

- GitHub Pages: `https://manatocookietwitter-lang.github.io/training-menu-app/`
- Repository: `manatocookietwitter-lang/training-menu-app`
- Default branch: `main`
- 確認時HEAD: `53d72586964a159682907196ce076bb517a47625`
- HEAD日時: 2026-09-05 15:11:25 UTC（2026-09-06 00:11:25 JST）
- HEADメッセージ: `Unify home menu button colors`
- 直前コミット: `b9466eb95e9f4a4fff5b7755d65ae95ca4e996cf` (`Refine training menu app UI flow`)

GitHubの現行 `index.html` / `app.js` と、`a1eac7f...53d7258` のコミット差分を確認し、少なくとも以下を今回の統合実装へ反映した。

- ホーム4入口：`メニュー表作成 / メニュー一覧 / 分類一覧 / 練習メニュー履歴`。4入口は同一系統の配色。
- `1 メニュー / 2 セット / 3 出力` の3段階フロー。
- 1〜5人から最大2パターンを選ぶ人数比較。
- セット種目は `秒数 × set数 × 人数` を人数パターンごとに計算。
- `1回のみ` の種目は各人数パターンへ固定秒数を1回加算。
- 人setを人数パターンごとに表示し、メニュー単位で一致しない場合を警告。
- 分・秒入力、実施方法 `セットあり / 1回のみ`、入力欄内エラー。
- メニュー通常タップ編集、編集モードで同分類内の並び替え＋削除、削除Undo。
- 分類通常タップ編集、編集モードで並び替え＋削除、削除Undo。
- 履歴通常タッププレビュー、編集モード削除、履歴削除の確認。
- 画像保存時の `履歴にも追加する` チェックと重複追加防止。
- 保存/削除等のトースト通知とフォーカス復帰。
- PWAの縦向き指定 (`portrait-primary`) は従来どおり維持。

統合アプリ側では、この現行GitHubの挙動を優先しつつ、部活運営アプリ固有の共有DB・予定連携・編集可能な保存表を残している。

## 過去版の互換確認

以前取得した `training_menu_app(8).html` は `references/training_menu_legacy.html` として保持している。これは現行GitHubの正本ではない。

旧版の `calcTotals()` と互換用フィールドについては200組の決定的入力で回帰比較し、旧データの読込・保存済みスナップショットを壊さないことを確認する。一方、現在の画面に表示する所要時間と人setは現行GitHubの人数パターン別ロジックを正とする。

## 予定 — 公開Siteは特定済み、現在ソースの直接取得は未完了

ユーザー指定の公開Siteは次。

- `https://hitotsu-calendar.manato0618.chatgpt.site`

この実行環境では `chatgpt.site` の現在ソースを直接取得する経路を確立できなかったため、今回の成果物について「2026-09-05時点のSiteコードを行単位で完全移植した」とは扱わない。

一方、取得済みの統合実装に存在する予定機能（月移動、予定追加、複数日/期間登録、保存、PNG出力）と、会話で決定した統合仕様（部活/幹部の重ね表示、同日複数、時刻なし、タスク期限投影、学年別参加者、画像出力）を保持し、自動試験を実施している。

## 今回の位置づけ

練習メニューについては「現行GitHubとの差分を確認して統合済み」。予定については「公開Site URLと既知の挙動を基準に統合・回帰済みだが、現在デプロイ中のSiteソースとのコード単位差分は未完了」。

公開URLへの再配置、既存GitHubへのコミット/PRは今回行っていない。
